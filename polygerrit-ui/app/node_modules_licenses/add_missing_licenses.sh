#!/bin/bash

SCRIPT_DIR=$(dirname "$0")
#Make the directory with this script current directory
cd "$SCRIPT_DIR"
(cd ../node_modules && find . -name "package.json" -printf '%h\n') | xargs -n1 -i sh -c 'mkdir -p {} && cp -n default-license.json {}/license.json'
