#!/bin/sh

set -eu

# /dev/urandom would be better, but it has to work on Mac too.
random=$((whoami ; hostname ; date) | sha1sum | awk '{print $1;}' )

git interpret-trailers --in-place --no-if-exists --trailer "Change-Id: I$random" $1
