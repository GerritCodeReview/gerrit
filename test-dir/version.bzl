# Maven style API version (e.g. '2.x-SNAPSHOT').
# Used by :api_install and :api_deploy targets
# when talking to the destination repository.
#
# Making a change to create a diff from symlink
GERRIT_VERSION = "3.3.3-SNAPSHOT"
